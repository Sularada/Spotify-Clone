import { createTheme } from "@mui/material/styles";
const theme = createTheme({
  palette: {
    dark: {
      main: "#000000",
      light: "#000000",
      dark: "#000000d0",
      contrastText: "#ffffff",
    },
  },
});
export default theme;
