import { Stack, styled } from "@mui/material";

const StyledAvatar = styled(Stack)({
  backgroundColor: "black",
  width: "fit-content",
  borderRadius: "30px",
  alignItems: "center",
  padding: "2px",
});
export default StyledAvatar;
